# 深度學習框架 (CUDA 11.8 支援 RTX A2000)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# 基本數據處理
numpy>=1.21.0
pandas>=1.3.0

# 機器學習工具
scikit-learn>=1.0.0

# 視覺化
matplotlib>=3.4.0
seaborn>=0.11.0

# 電腦視覺 (可選，用於影像處理)
opencv-python>=4.5.0
pillow>=8.0.0

# 手部檢測 (可選，用於骨架檢測)
mediapipe>=0.10.0

# 進度條和工具
tqdm>=4.62.0

# 更快的數據載入 (可選)
h5py>=3.1.0

# Jupyter 支援 (可選，用於開發)
jupyter>=1.0.0
notebook>=6.4.0
ipykernel>=6.0.0

# 模型監控 (可選)
tensorboard>=2.8.0

# 設定說明：
# - torch: 使用 CUDA 11.8 版本以支援 RTX A2000
# - 安裝指令: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# - 其他套件可直接用 pip install 安裝